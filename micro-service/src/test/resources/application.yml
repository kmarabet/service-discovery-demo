#Could be also declared in the @WebIntegrationTest({"server.port=0", "spring.cloud.discovery.enabled=false"})
server:
#  port: 2222   # HTTP (Tomcat) port
  port: 0

# Spring properties
spring:
  application:
     name: micro-service  # Service registers under this name
